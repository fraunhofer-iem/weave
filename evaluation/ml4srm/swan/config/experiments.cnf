mem.max = 4048
cpu.max = 12
ignore.time = .*
ignore.memory = .*

keyfields = seeds:int(3), classifier:varchar(400)

seeds = 986,2975
classifier = automl

resultfields = automlClassifier:varchar(400), info:varchar(1000), avgPrecision:double, avgRecall:double, avgFMeasure:double, microRecall:double, microPrecision:double, microFMeasure:double, weightedFMeasure:double,lblPrecision:varchar(200), lblRecall:varchar(200), lblHarmonic:varchar(200), lblTruePositive:varchar(200), lblFalsePositive:varchar(200), lblTrueNegative:varchar(200), lblFalseNegative:varchar(200)


